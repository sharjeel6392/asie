model_name: distilbert-base-uncased
epochs: 3
batch_size: 16
test_size: 0.2
lr: 2e-5
max_length: 128
seed: 42
artifact_dir: artifacts
experiment_name: ASIE_Week1
model_dir: ${artifact_dir}/model
model_file: asie_model.pkl
logging_dir: ${artifact_dir}/logs
