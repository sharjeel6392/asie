[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "asie"
version = "0.1.0"
description = "Automated Sentiment Identification Engine"
requires-python = ">=3.9"

[tool.setuptools]
packages = ["src"]