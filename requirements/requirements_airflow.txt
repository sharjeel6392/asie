# Airflow image (Dockerfile.airflow) — full training/tracking stack.
# torch is installed separately (CPU wheel, see Dockerfile.airflow) so
# it's not listed here.
transformers==4.57.6
# Required by src/models/hf_model.py, which the retraining DAG imports via
# src.models.factory. It was dropped when requirements_inference.txt was split
# into serving/airflow sets, so the DAG failed at import with
# ModuleNotFoundError and Airflow listed zero DAGs -- the scheduler and
# webserver were both healthy, which is why it wasn't obvious.
# Not restoring the old datasets==2.21.0 pin: that predates the numpy 2.x
# pinned below. 5.0.1 is what pip resolves against numpy 2.4.1 / pyarrow 22,
# pinned here so the image stays reproducible.
datasets==5.0.1
scikit-learn==1.8.0
pandas==2.3.3
numpy==2.4.1
scipy
pyyaml==6.0.3

# --- Data Layer & Versioning ---
pyarrow==22.0.0
accelerate==1.12.0
fastparquet==2024.2.0
dvc==3.48.4
dvc-s3

# --- AWS ---
boto3

# --- Storage (Postgres via RDS; SQLite locally) ---
# Airflow 2.10.x is NOT compatible with SQLAlchemy 2.x -- its ORM models use
# legacy (non-Mapped[]) annotations, so 2.x raises MappedAnnotationError on
# TaskInstance.dag_model and the migration job dies before creating a single
# table. This was pinned to 2.* during the Postgres port, which silently
# broke Airflow while leaving the serving image fine.
# src/db/engine.py passes future=True so its 2.0-style API works on 1.4 too.
SQLAlchemy>=1.4.36,<2.0
psycopg2-binary

# --- Experiment Tracking & Observability ---
mlflow==3.9.0

# --- Testing & Quality Assurance ---
pytest==9.0.2
httpx==0.28.1

# --- Monitoring tools ---
prometheus_client==0.25.0

# --- Local Development & Environment Management ---
python-dotenv==1.2.1
tqdm==4.67.1
