# Serving image (dockerfile) — torch is installed separately (CPU wheel,
# see dockerfile) so it's not listed here. Keep this file to what
# src/serving and src/drift actually import; training/tracking/dev tooling
# (mlflow, dvc, datasets, pytest, httpx, accelerate, pyarrow, fastparquet,
# tqdm, boto3) belongs in requirements_airflow.txt instead.
transformers==4.57.6
scikit-learn==1.8.0
pandas==2.3.3
numpy==2.4.1
scipy
pyyaml==6.0.3

# --- Serving & Inference Layer ---
fastapi==0.128.0
uvicorn[standard]==0.40.0
pydantic==2.12.5
python-multipart

# --- Storage (Postgres via RDS; SQLite locally) ---
SQLAlchemy==2.*
psycopg2-binary

# --- Monitoring tools ---
prometheus_client==0.25.0

# --- Local Development & Environment Management ---
python-dotenv==1.2.1
